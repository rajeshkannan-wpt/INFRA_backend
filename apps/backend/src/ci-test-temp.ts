// TEMP FILE — CI VERIFICATION TEST FOR INFRA-1.3-V — SAFE TO DELETE AFTER TEST
const ciTestUnusedVariable = "this variable is intentionally unused to trigger a lint error";

const ciTestTypeError: number = "this is a string assigned to a number to trigger a typecheck error";
