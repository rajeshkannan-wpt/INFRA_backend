# QR login helper (apps/web/qr-login) — used when running it standalone
PORT=3000

# Reachable base URL used to build the scan QR (LAN IP). WPT_PUBLIC_BASE_URL is the fallback.
PUBLIC_BASE_URL=
WPT_PUBLIC_BASE_URL=

# Web app (Next.js) — reserved for the future mock-API swap
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4000